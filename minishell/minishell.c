#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include <stdint.h>

int main()
{
    while(1){
        printf("[user@localhost] $ ");
        fflush(stdout);
        char cmd_buf[1024] = { 0 };
        
    }
    return 0;
}
